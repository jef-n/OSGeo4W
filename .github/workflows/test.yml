name: test

on:
#  schedule:
#  - cron: '2 0 * * * *'
#
  push:
    branches:
    - workflows



jobs:
 build:
    name: test
    runs-on: windows-latest

    env:
      TEMP: c:\t
      BASE: c:\a

    steps:
    - name: override
      shell: cmd
      run: |
       mkdir %BASE%
       dir /s /b %GITHUB_WORKSPACE%
       dir %GITHUB_WORKSPACE%
       echo CD:%CD%
       dir %GITHUB_ENV%
       mkdir %BASE%
       dir %BASE%
       rd %GITHUB_WORKSPACE%
       mklink /j %GITHUB_WORKSPACE% %BASE%

    - name: changed?
      shell: cmd
      run: |
        echo GITHUB_WORKSPACE=%GITHUB_WORKSPACE%
        dir /s /b %GITHUB_WORKSPACE%
        dir /s /b %BASE%

# vim: set nowrap :
